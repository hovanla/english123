"use client";
import Link from "next/link";
import { useState } from "react";
export default function ForgotPasswordPage() { const [message, setMessage] = useState(""); const [url, setUrl] = useState(""); return <main className="grid min-h-screen place-items-center bg-emerald-50 p-4"><form onSubmit={async (event) => { event.preventDefault(); const email = new FormData(event.currentTarget).get("email"); const response = await fetch("/api/auth/forgot-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); const data = await response.json(); setMessage("Nếu email tồn tại, chúng tôi đã gửi liên kết đặt lại mật khẩu."); setUrl(data.developmentResetUrl || ""); }} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-xl"><Link href="/login" className="font-black text-emerald-700">← Đăng nhập</Link><h1 className="mt-6 text-3xl font-black">Quên mật khẩu</h1><input required type="email" name="email" placeholder="Email phụ huynh" className="mt-5 w-full rounded-xl border p-3"/><button className="mt-4 w-full rounded-xl bg-emerald-700 p-3 font-black text-white">Gửi liên kết</button>{message && <p className="mt-4 text-sm">{message}</p>}{url && <a className="mt-3 block break-all text-sm font-bold text-emerald-700" href={url}>Mở liên kết thử nghiệm</a>}</form></main>; }
