"use client";
import { useSearchParams, useRouter } from "next/navigation";
import { useState } from "react";
export default function ResetPasswordForm() { const params = useSearchParams(); const router = useRouter(); const [error, setError] = useState(""); return <form onSubmit={async (event) => { event.preventDefault(); const password = new FormData(event.currentTarget).get("password"); const response = await fetch("/api/auth/reset-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ token: params.get("token"), password }) }); if (!response.ok) return setError("Liên kết đã hết hạn hoặc không hợp lệ."); router.push("/login"); }} className="w-full max-w-md rounded-3xl bg-white p-7 shadow-xl"><h1 className="text-3xl font-black">Tạo mật khẩu mới</h1><input required minLength={8} type="password" name="password" placeholder="Ít nhất 8 ký tự" className="mt-5 w-full rounded-xl border p-3"/>{error && <p className="mt-3 text-sm text-red-700">{error}</p>}<button className="mt-4 w-full rounded-xl bg-emerald-700 p-3 font-black text-white">Đổi mật khẩu</button></form>; }
